package gnu.client.module.modules.combat.velocity;

import gnu.client.module.modules.combat.VelocityModule;

/**
 * OpenMiau Rise stub — OMDelay covers the active delay path; this mode is a no-op placeholder.
 */
public final class DelayVelocity extends VelocityMode {

    public DelayVelocity(VelocityModule parent) {
        super("Delay", parent);
    }
}
