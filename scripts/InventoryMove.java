void patchMovementInput(Object movInput) {
    // MovementInput is passed through; inventory-move bypass lives in mixins.
    // This hook is available for script-side movement overrides while GUI is open.
}
